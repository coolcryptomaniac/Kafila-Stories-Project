/* ============================================================
   KAFILA STORIES — the only file you must edit to go live.
   See SETUP.md for how to get each value.
   ============================================================ */
window.KAFILA_CONFIG = {

  /* 1. Google Apps Script Web App URL.
     Paste apps-script/Code.gs into script.google.com, deploy as a
     Web App ("Execute as: Me", "Who has access: Anyone"), then put
     the /exec URL here. Until you do, the booking form runs in
     demo mode and shows a message instead of saving. */
  API_URL: "",

  /* 2. GitHub repo, used by the admin panel to save content edits.
     Format: "username/repository" */
  REPO: "your-username/kafila-stories",
  BRANCH: "main",

  /* 3. Site basics */
  SITE_URL: "https://your-username.github.io/kafila-stories",
  DEMO_MODE_NOTICE: true
};
